apiVersion: v1
kind: Service
metadata:
  name: projecttasks-service
  namespace: projecttasks
spec:
  selector:
    app: projecttasks
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8050